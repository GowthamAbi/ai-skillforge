import {useEffect,useState} from "react";import {api} from "../api/api";import ProgressChart from "../components/ProgressChart";import SkillProgress from "../components/SkillProgress";import ScoreCard from "../components/ScoreCard";export default function Analytics(){const [data,setData]=useState(null);useEffect(()=>{api('/dashboard').then(setData).catch(console.error)},[]);if(!data)return <p>Loading analytics…</p>;const avg=data.days.length?Math.round(data.days.reduce((s,d)=>s+(d.score||0),0)/data.days.length):0;return <div className="mx-auto max-w-6xl"><h2 className="text-3xl font-black">Analytics</h2><p className="mt-1 text-slate-400">Track learning, skills and mentor scores across all 50 days.</p><div className="mt-6 grid gap-4 lg:grid-cols-3"><ScoreCard score={avg}/><div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5"><p className="text-sm text-slate-400">Study hours</p><div className="mt-2 text-5xl font-black">{(data.totalMinutes/60).toFixed(1)}</div><p className="mt-2 text-cyan-300">of 250-hour target</p></div><div className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5"><p className="text-sm text-slate-400">Task completion</p><div className="mt-2 text-5xl font-black">{data.completion}%</div><p className="mt-2 text-cyan-300">50-day roadmap</p></div></div><div className="mt-4 grid gap-4 lg:grid-cols-2"><SkillProgress days={data.days}/><ProgressChart days={data.days}/></div></div>}
