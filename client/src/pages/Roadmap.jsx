import {useEffect,useState} from "react";import {api} from "../api/api";export default function Roadmap(){const [days,setDays]=useState([]);useEffect(()=>{api('/days').then(setDays).catch(console.error)},[]);return <div className="mx-auto max-w-5xl"><h2 className="text-3xl font-black">50-Day Roadmap</h2><p className="mt-1 text-slate-400">Your complete AI Engineering learning path.</p><div className="mt-6 grid gap-3">{days.map(d=>{const done=(d.tasks||[]).filter(t=>t.completed).length,total=(d.tasks||[]).length,pct=total?Math.round(done/total*100):0;return <div key={d.day} className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4"><div className="flex items-center gap-4"><div className="grid h-12 w-12 place-items-center rounded-xl bg-slate-800 font-black">{d.day}</div><div className="min-w-0 flex-1"><div className="flex justify-between gap-3"><b>{d.topic}</b><span className="text-sm text-slate-400">{pct}%</span></div><div className="mt-2 h-2 rounded-full bg-slate-800"><div className="h-2 rounded-full bg-cyan-400" style={{width:`${pct}%`}}/></div></div></div></div>})}</div></div>}
