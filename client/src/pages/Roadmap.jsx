export default function Roadmap({days,onSelect}){return <div><div className="mb-6"><p className="text-sm font-bold text-cyan-400">50-DAY ROADMAP</p><h2 className="text-3xl font-black">What to learn + what to build</h2><p className="mt-2 text-slate-400">Every day has a fixed 9 PM–2 AM timetable and a concrete project deliverable.</p></div><div className="grid gap-3">{days.map(d=>{const done=d.tasks.filter(t=>t.completed).length,p=Math.round(done/d.tasks.length*100);return <button key={d.day} onClick={()=>onSelect(d.day)} className="rounded-2xl border border-slate-800 bg-slate-900/70 p-4 text-left hover:border-cyan-800"><div className="flex flex-wrap items-start justify-between gap-3"><div><span className="text-xs font-bold text-cyan-400">DAY {d.day}</span><h3 className="text-lg font-bold">{d.topic}</h3><p className="mt-1 text-sm text-slate-400">{d.learningGoal}</p><p className="mt-2 text-sm"><span className="font-bold text-violet-300">Build:</span> {d.project}</p></div><div className="min-w-24 text-right"><b>{p}%</b><p className="text-xs text-slate-500">{done}/{d.tasks.length} tasks</p></div></div></button>})}</div></div>}