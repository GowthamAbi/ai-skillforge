const groups=[['Foundation',1,10],['RAG',11,17],['Fine-tuning',18,23],['Agents',24,38],['Production',39,45],['Interview',46,50]];
export default function SkillProgress({days=[]}){return <section className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5"><h2 className="text-xl font-bold">Skill progress</h2><div className="mt-5 space-y-4">{groups.map(([name,a,b])=>{const selected=days.filter(d=>d.day>=a&&d.day<=b);const all=selected.flatMap(d=>d.tasks||[]);const pct=all.length?Math.round(all.filter(t=>t.completed).length/all.length*100):0;return <div key={name}><div className="mb-1 flex justify-between text-sm"><span>{name}</span><span>{pct}%</span></div><div className="h-2 rounded-full bg-slate-800"><div className="h-2 rounded-full bg-cyan-400" style={{width:`${pct}%`}}/></div></div>})}</div></section>}
