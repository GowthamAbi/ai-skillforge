import {useEffect,useState} from "react";
export default function StudyTimer({minutes=0,onSave}){const [value,setValue]=useState(minutes);const [seconds,setSeconds]=useState(0);const [running,setRunning]=useState(false);useEffect(()=>setValue(minutes),[minutes]);useEffect(()=>{if(!running)return;const id=setInterval(()=>setSeconds(s=>s+1),1000);return()=>clearInterval(id)},[running]);const shown=`${String(Math.floor(seconds/3600)).padStart(2,'0')}:${String(Math.floor(seconds%3600/60)).padStart(2,'0')}:${String(seconds%60).padStart(2,'0')}`;return <section className="rounded-2xl border border-slate-800 bg-slate-900/70 p-5"><h2 className="text-xl font-bold">Study timer</h2><p className="mt-1 text-sm text-slate-400">Daily target: 300 minutes</p><div className="my-5 text-center font-mono text-4xl font-black">{shown}</div><div className="flex gap-2"><button onClick={()=>setRunning(v=>!v)} className="rounded-xl bg-cyan-500 px-4 py-2 font-bold text-slate-950">{running?'Pause':'Start'}</button><button onClick={()=>{setRunning(false);setSeconds(0)}} className="rounded-xl bg-slate-800 px-4 py-2">Reset</button></div><div className="mt-5 flex gap-2"><input value={value} onChange={e=>setValue(e.target.value)} type="number" min="0" className="min-w-0 flex-1 rounded-xl bg-slate-800 p-3"/><button onClick={()=>onSave(Number(value))} className="rounded-xl bg-violet-500 px-4 font-bold">Save</button></div></section>}
